{
  "name": "formflow-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply formflow --local",
    "db:migrate": "wrangler d1 migrations apply formflow --remote"
  },
  "devDependencies": {
    "wrangler": "^4.137.0"
  }
}
